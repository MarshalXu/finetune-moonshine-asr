# GPU LoRA fine-tune config for Arabic Moonshine baseline.
# Optimized for Nvidia GPUs (enables fp16, bumps batch size for high throughput).

model:
  name: "./model/moonshine-base-ar"
  freeze_encoder: false

peft:
  enabled: true
  r: 8
  lora_alpha: 16
  lora_dropout: 0.05
  bias: "none"
  target_modules:
    - "q_proj"
    - "v_proj"

dataset:
  type: "local"
  path: "./datasets/whisper_ar_manifist0508_hf"
  text_column: "sentence"
  cast_audio: false

audio:
  sampling_rate: 16000
  max_duration: 20.0
  min_duration: 0.5

preprocessing:
  num_proc: null
  normalize_audio: false

curriculum:
  enabled: false

training:
  output_dir: "./model/results-moonshine-base-ar-lora-gpu"

  per_device_train_batch_size: 16
  per_device_eval_batch_size: 16
  gradient_accumulation_steps: 2  # Effective Batch Size = 16 * 2 = 32
  gradient_checkpointing: true
  fp16: true
  fp16_full_eval: true
  group_by_length: true
  length_column_name: "input_length"
  max_grad_norm: 1.0

  num_train_epochs: 6
  max_steps: -1
  optim: "adamw_torch"
  learning_rate: 1.0e-4
  lr_scheduler_type: "linear"
  warmup_steps: 50

  eval_strategy: "epoch"
  eval_steps: null
  save_strategy: "epoch"
  save_steps: null
  save_total_limit: 3
  logging_steps: 10
  predict_with_generate: true

  load_best_model_at_end: true
  metric_for_best_model: "wer"
  greater_is_better: false

  report_to: []
  logging_dir: "./logs/moonshine-base-ar-lora-gpu"

  push_to_hub: false
  hub_model_id: null
  hub_strategy: "every_save"
  hub_token: null

generation:
  max_new_tokens: 50
  length_penalty: 1.2
  do_sample: false
  early_stopping: true
  repetition_penalty: 1.1
  num_beams: 1
  no_repeat_ngram_size: 0
